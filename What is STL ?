# include <bits/stdc++.h>
using namespace std;
int main()
{
    int a=5, b=8, maximum;
    // maximum from a and b
    maximum=max(a, b);

    //swapping two values
    swap(a, b); // a=8, b=5;

    return 0;
}
